import { KatexRendererDirective } from './katex-renderer.directive';

describe('KatexRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new KatexRendererDirective();
    expect(directive).toBeTruthy();
  });
});
