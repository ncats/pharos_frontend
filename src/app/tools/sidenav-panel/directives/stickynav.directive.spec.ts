import { StickynavDirective } from './stickynav.directive';

describe('StickynavDirective', () => {
  it('should create an instance', () => {
    const directive = new StickynavDirective();
    expect(directive).toBeTruthy();
  });
});
