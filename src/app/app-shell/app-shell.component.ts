import {Component} from '@angular/core';

/**
 * shell component used for service workers and angular PWA apps
 * This is an initial app skeleton that loads, and is then replaced by the functional app
 */
@Component({
  selector: 'app-shell',
  templateUrl: './app-shell.component.html',
  styleUrls: ['./app-shell.component.scss']
})
export class AppShellComponent {}
